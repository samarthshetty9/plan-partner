# Netlify: frontend build + API proxy via Vite env
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "node generate-redirects.js && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "/api"
